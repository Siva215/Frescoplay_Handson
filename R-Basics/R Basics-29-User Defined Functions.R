#====================================================
# R Basics-29-User Defined Functions
#====================================================

# Enter your code here. Read input from STDIN. Print output to STDOUT
pyth <- function(h,l)
{
  return(sqrt(h*h + l*l))
}


print(pyth(3,4))
print(pyth(6,8))
